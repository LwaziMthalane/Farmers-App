name: Build Kivy APK with Proper Signing

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: DEBUG - Show keystore
        run: |
          echo "Keystore in repo:"
          ls -la android/debug.keystore
          echo "MD5: $(md5sum android/debug.keystore | cut -d' ' -f1)"

      - name: Build APK using Buildozer Action
        uses: digreatbrian/buildozer-action@v2
        with:
          buildozer-cmd: |
            buildozer -v android debug

      - name: PROPER SIGNING with apksigner
        run: |
          # Install Android SDK command line tools
          echo "📥 Installing Android SDK tools..."
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q commandlinetools-linux-*.zip
          mkdir -p android-sdk/cmdline-tools
          mv cmdline-tools android-sdk/cmdline-tools/latest
          
          # Setup environment
          export ANDROID_SDK_ROOT=$PWD/android-sdk
          export PATH=$PATH:$ANDROID_SDK_ROOT/cmdline-tools/latest/bin
          
          # Install build-tools (includes apksigner)
          echo "📦 Installing build-tools..."
          yes | sdkmanager --install "build-tools;34.0.0" --sdk_root=$ANDROID_SDK_ROOT
          export PATH=$PATH:$ANDROID_SDK_ROOT/build-tools/34.0.0
          
          # Find APK
          APK=$(find ./bin -name "*.apk" | head -1)
          if [ ! -f "$APK" ]; then
            echo "❌ No APK found!"
            exit 1
          fi
          
          echo "📦 APK: $(basename "$APK")"
          
          # Remove existing signatures
          zip -d "$APK" "META-INF/*" 2>/dev/null || true
          
          # Sign with apksigner
          echo "🔏 Signing..."
          apksigner sign \
            --ks android/debug.keystore \
            --ks-pass pass:android \
            --key-pass pass:android \
            --ks-key-alias androiddebugkey \
            "$APK"
          
          echo "✅ Signed with apksigner"

      - name: Check final APK
        run: |
          APK=$(find ./bin -name "*.apk" | head -1)
          if [ -f "$APK" ]; then
            echo "📱 Final APK: $(basename "$APK")"
            echo "📏 Size: $(wc -c < "$APK") bytes"
            
            # Check if it has proper signature
            if command -v apksigner >/dev/null 2>&1; then
              echo "🔑 Signature check:"
              apksigner verify --print-certs "$APK" | head -20 || true
            fi
          fi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: vowle-properly-signed
          path: ./bin/*.apk
          retention-days: 1
